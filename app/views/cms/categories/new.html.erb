<h1>New Category</h1>

<%= form_with model: @category, url: cms_categories_path do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.submit 'Create Category' %>
<% end %>

<%= link_to 'Back', cms_categories_path %>