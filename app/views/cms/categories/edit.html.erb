<h1>Edit Category</h1>

<%= form_with model: @category, url: cms_category_path(@category), method: :patch do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.submit 'Update Category' %>
<% end %>

<%= link_to 'Back', cms_category_path(@category) %>
