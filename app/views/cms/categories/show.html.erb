<h1>Category Details</h1>

<p><strong>Name:</strong> <%= @category.name %></p>

<%= link_to 'Edit', edit_cms_category_path(@category) %> |
<%= link_to 'Back', cms_categories_path %>
<%= link_to 'Delete', cms_category_path(@category), method: :delete, data: { confirm: 'Are you sure?' } %>